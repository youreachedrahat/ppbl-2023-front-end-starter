# Mastery Assignment 102.2

Update your lucky number.

For this mastery assignment, we will take a look at your Contributor Token.

CIP 68

What our data looks like

How to change it

We will look at Contracts soon. You can review them here. For now, we will use a pre-compiled Plutus Script / reference UTxO.

import MDXLessonLayout from "@/src/components/lms/Lesson/MDXLessonLayout.tsx";
export default ({ children }) => <MDXLessonLayout>{children}</MDXLessonLayout>;